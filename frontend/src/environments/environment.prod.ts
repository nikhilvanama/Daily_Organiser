// Production environment — update apiUrl to your Render backend URL after deployment
export const environment = {
  production: true,
  apiUrl: 'https://daily-organiser-api.onrender.com/api', // Update this after Render deployment
};
